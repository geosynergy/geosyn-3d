export default class BasePlane {
  constructor (minX, minY, planeWidth, planeHeight) {
    this.minX = minX
    this.minY = minY
    this.planeWidth = planeWidth
    this.planeHeight = planeHeight
  }
}